model Booking {
  id         Int      @id @default(autoincrement())
  type       String
  reg        String
  make_model String
  datetime   DateTime
  location   String
  notes      String?
  createdAt  DateTime @default(now())
}